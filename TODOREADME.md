Maybe start a new github repository and make sure client folder(which isn't included for some reason is uploaded as well as server folder)

Hide sensitive data
    - Node Modules
    - Db Connection config